[package]
name = "nautrix_adblock"
version = "0.1.0"
edition = "2024"
license = "MPL-2.0"

[lib]
crate-type = ["cdylib"]

[dependencies]
adblock = { version = "=0.13.3", default-features = false, features = ["embedded-domain-resolver", "full-regex-handling"] }
jni = "0.21.1"
serde_json = "1.0.149"

[profile.release]
lto = "thin"
codegen-units = 1
panic = "abort"
strip = "symbols"
